{
  "buildCommand": "cd digitek-platform && npm install && npx vite build",
  "outputDirectory": "digitek-platform/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
